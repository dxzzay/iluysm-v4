const config = require("../config.json");
const edit = require("../edit.json");

const info = "[ " + "INFO".red + " ] - "
const error = "[ " + "ERROR".red + " ] - "
const success = "[ " + "SUCCESS".red + " ] - "

module.exports = async (bot, member) => {

    /*console.log(info + `O membro ['${member.user.username}'] entrou no servidor ['${member.guild.name}'].`);
    member.send(edit.uMessage3)
    .catch(errorr => console.log(error + `Não foi possivel enviar mensagem para ['${member.user.usernam}'].`));*/
}

